spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kpdhadge/common-external-configuration
          default-label: main
    fail-fast: true

debug: true

server:
  port: 8888